# Things that should be tested to check if everything works
* Filling in data in all fields -> Several rows, and also selecting non-default values for all comboboxes -> then exporting to XML and checking if the data looks correctly
* Loading the XML and checking if all comboboxes get set correctly